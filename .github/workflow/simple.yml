name: Shell Commands
on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
  permissions:
    issues: write
    steps: 
      - name: echo a string
        run: echo "hello everyone"
      - name: multiline script
        run: |
         node -v
         npm -v 
