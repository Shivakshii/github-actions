name: Shell Commands
on: [push]

permissions:
  issues: write

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps: 
    - name: echo a string
      run: echo "hello everyone"
    - name: multiline script
      run: |
       node -v
       npm -v

    
